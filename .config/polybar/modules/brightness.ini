[module/brightness]
type = internal/xbacklight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
card = intel_backlight

; Available tags:
;   <label> (default)
;   <ramp>
;   <bar>
format = <ramp> <bar>

; Available tokens:
;   %percentage% (default)
label = %percentage%%

; Only applies if <ramp> is used

ramp-0 = 
ramp-1 = 
ramp-2 = 
ramp-3 = 
ramp-4 = 

; Only applies if <bar> is used
bar-width = 10
bar-gradient = false

bar-indicator = 
bar-indicator-foreground = ${xrdb:bi}
bar-indicator-font = 2

bar-fill = ━
bar-foreground-0 = ${xrdb:bn}
bar-foreground-1 = ${xrdb:bn}
bar-foreground-2 = ${xrdb:bn}
bar-foreground-3 = ${xrdb:bm}
bar-foreground-4 = ${xrdb:bm}
bar-foreground-5 = ${xrdb:bd}
bar-foreground-6 = ${xrdb:bd}
bar-foreground-7 = ${xrdb:bd}
bar-foreground-8 = ${xrdb:bd}
bar-fill-font = 2

bar-empty = ┉
bar-empty-foreground = ${xrdb:be}
bar-empty-font = 2
